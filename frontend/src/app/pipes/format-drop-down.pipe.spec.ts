import { FormatDropDownPipe } from './format-drop-down.pipe';

describe('FormatDropDownPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDropDownPipe();
    expect(pipe).toBeTruthy();
  });
});
